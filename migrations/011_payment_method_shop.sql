-- Два магазина ЮКассы: бот остаётся на исходном магазине, сайт svoiweb.ru
-- переезжает на отдельный (требование ЮКассы: один магазин = один канал продаж).
-- Сохранённая карта (payment_method_id) списывается ТОЛЬКО через магазин,
-- который её выдал, поэтому запоминаем принадлежность: 'bot' | 'web'.
-- NULL трактуется как 'bot' (все карты до миграции сохранены ботовским магазином).
ALTER TABLE users ADD COLUMN IF NOT EXISTS payment_method_shop TEXT;
